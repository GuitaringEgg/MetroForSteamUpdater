# -*- mode: python -*-
a = Analysis(['gui.py', 'updater.py'],
             pathex=['D:\\dev\\MetroForSteamUpdater'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='MetroForSteamUpdater.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='MetroForSteamUpdater')
